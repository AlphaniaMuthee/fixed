import { DateEntryPipe } from './date-entry.pipe';

describe('DateEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new DateEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
